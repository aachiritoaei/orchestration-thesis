server:
  port: 5002

spring:
  application:
    name: io-server

kafka:
  bootstrap-servers: kafka-host:9092
  topic:
    kafka-io: kafka-io

eureka:
  instance:
    preferIpAddress: true
  client:
    serviceUrl:
      defaultZone: http://discovery-host:8761/eureka/
